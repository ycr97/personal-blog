---
author: "ycr97"
pubDatetime: 2021-01-06T00:00:00+08:00
title: "面试题"
draft: false
tags: ["面试"]
category: "面试与复盘"
description: "2020/10/15面试总结 笔试题: 以下代码执行结果? 1. 静态代码块在类加载的时候执行一次,所以最先执行随后都不会执行.其次子类初始化实例的时候会先调用父类构造器再调用子类构造器 以下代码不能执行,编译不能通过,throw后的代码块不能执行到,类似于return后的代码一样 2. String,StringBuffer,StringBuilde..."
---

## 2020/10/15面试总结
##### 笔试题:
###### 以下代码执行结果?
1. 静态代码块在类加载的时候执行一次,所以最先执行随后都不会执行.其次子类初始化实例的时候会先调用父类构造器再调用子类构造器

```java
public class Test {

    public static void main(String[] args) {// 1B2b2b
        A b = new B();
        b= new B();
    }

}

class A {
    static {
        System.out.print(1);
    }

    public A() {
        System.out.print(2);
    }
}

class B extends A {
    static {
        System.out.print("B");
    }

    public B() {
        System.out.print("b");
    }
}

```
以下代码不能执行,编译不能通过,throw后的代码块不能执行到,类似于return后的代码一样
```java
   public static void main(String[] args) {
        try {
            throw new BB();
            System.out.println(111);
        }catch (AA a) {
            System.out.println("1" + a.getMessage());
        }
        catch (Exception e) {
            System.out.println("2" + e.getMessage());
        }
    }
}

class AA extends Exception {

}

class BB extends AA {

}

```
2. String,StringBuffer,StringBuilder性能排序,StringBuilder和StringBuffer哪个是线程不安全的
性能:StringBuilder>StringBuffer>String.线程不安全 StringBuilder

#### 3. MySQL索引类型
normal：表示普通索引
unique：表示唯一的，不允许重复的索引，如果该字段信息保证不会重复例如身份证号用作索引时，可设置为unique
full textl: 表示 全文搜索的索引。 FULLTEXT 用于搜索很长一篇文章的时候，效果最好。用在比较短的文本，如果就一两行字的，普通的 INDEX 也可以。

总结，索引的类别由建立索引的字段内容特性来决定，通常normal最常见。
#### 4. MySQL数据库事务隔离级别,脏读,不可重复读,幻读
![面试题 图 1](/personal-blog/images/csdn/109106259/01-7d57d78a7505.png)
Read UnCommitted(读未提交)
最低的隔离级别。一个事务可以读取另一个事务并未提交的更新结果。

Read Committed(读提交)
大部分数据库采用的默认隔离级别。一个事务的更新操作结果只有在该事务提交之后，另一个事务才可以的读取到同一笔数据更新后的结果。

Repeatable Read(重复读)
mysql的默认级别。整个事务过程中，对同一笔数据的读取结果是相同的，不管其他事务是否在对共享数据进行更新，也不管更新提交与否。

Serializable(序列化)
最高隔离级别。所有事务操作依次顺序执行。注意这会导致并发度下降，性能最差。通常会用其他并发级别加上相应的并发锁机制来取代它。
8. left join 和 right join
9. Redis数据类型
10. cookie和session的区别
#### 2.面试
1. Redis中Hash的使用
2. Spring Cloud常用的一些组件
3. Getway的作用
4. Eureka的替代方案有哪些(Zookeeper,Alibaba Nacos)和Nacos的区别,分布式中的CAP原则
5. 分布式锁
6. 一个订单15分钟未支付则将其改为未支付状态解决方案
采用Redis的订阅

---

> 本文最初发布于 CSDN：[查看原文](https://blog.csdn.net/qq_42618152/article/details/109106259)。
